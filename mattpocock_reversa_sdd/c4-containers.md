# C4 Containers Diagram — skills

> Generated by Reversa Architect on 2026-05-15
> Level 2: Internal structure of the Skills Library
> Confidence: 🟢 CONFIRMED (structure) | 🟡 INFERIDO (boundaries)

---

## Diagram

```mermaid
C4Container
    title Skills Library — Container Diagram (C4 Level 2)

    Person(developer, "Developer", "Invokes skills via Claude Code")
    Person(maintainer, "Maintainer", "Authors and publishes skills")

    System_Boundary(skills_library, "Matt Pocock Skills Library") {

        Container(active_skills, "Active Skill Buckets", "Markdown / SKILL.md", "Published and actively maintained skills. Three sub-buckets: engineering/ (10 skills), productivity/ (4 skills), misc/ (4 skills). engineering/ and productivity/ are published; misc/ is unlisted.")

        Container(inprogress_skills, "In-Progress Bucket", "Markdown / SKILL.md", "Draft skills not yet ready to ship: review, writing-beats, writing-fragments, writing-shape. Not published. No formal graduation criteria.")

        Container(deprecated_skills, "Deprecated Bucket", "Markdown / SKILL.md", "Skills no longer actively used. Preserved for institutional knowledge. Excluded from link-skills.sh and plugin.json.")

        Container(personal_skills, "Personal Bucket", "Markdown / SKILL.md", "Skills tied to maintainer's own setup. Not published or linked.")

        Container(plugin_registry, "Plugin Registry", "JSON + Shell script", ".claude-plugin/plugin.json lists published skills. link-skills.sh creates symlinks in ~/.claude/skills/. Governs what Claude Code can discover.")

        Container(setup_config, "Setup Configuration Templates", "Markdown templates", "Files seeded by setup-matt-pocock-skills into the developer's target repo: triage-labels.md, CONTEXT.md seed, docs/adr/ structure. Per-repo, not stored in skills library after seeding.")

        Container(out_of_scope, "Out-of-Scope Registry", "Markdown files", ".out-of-scope/<concept>.md. Records rejected enhancement proposals (not bugs). Used by triage skill for concept-similarity matching to detect duplicate requests.")

        Container(docs, "Documentation & ADRs", "Markdown", "docs/adr/ — architectural decisions. README.md files — skill listings per bucket. CLAUDE.md — contributor governance rules. CONTEXT.md — project glossary.")
    }

    System_Ext(claude_code, "Claude Code", "AI agent runtime")
    System_Ext(npm_registry, "npm Registry", "npx skills@latest")
    System_Ext(issue_tracker, "Issue Tracker", "GitHub / GitLab / local-markdown")

    Rel(developer, claude_code, "Invokes slash commands")
    Rel(maintainer, active_skills, "Authors skills in", "SKILL.md files")
    Rel(maintainer, plugin_registry, "Updates registry when publishing/deprecating")

    Rel(claude_code, plugin_registry, "Discovers available skills via", "symlinks in ~/.claude/skills/")
    Rel(claude_code, active_skills, "Reads SKILL.md and supporting files from")
    Rel(claude_code, inprogress_skills, "Reads if explicitly linked")
    Rel(claude_code, setup_config, "Seeds into developer's target repo via", "setup-matt-pocock-skills skill")
    Rel(claude_code, issue_tracker, "Creates issues, applies labels, posts comments to")

    Rel(active_skills, out_of_scope, "triage skill reads for concept-similarity matching")
    Rel(plugin_registry, npm_registry, "Published via")
    Rel(deprecated_skills, out_of_scope, "Deprecation of enhancement skills creates entries in")
```

---

## Container Descriptions

### Active Skill Buckets
The core content of the library. Three publication tiers:
- **`engineering/`** (10 skills): code-work tools — diagnosis, TDD, triage, architecture, prototyping. Published in plugin.json.
- **`productivity/`** (4 skills): non-code workflow tools — caveman, grill-me, handoff, write-a-skill. Published in plugin.json.
- **`misc/`** (4 skills): kept but rarely used — git-guardrails, migrate-to-shoehorn, scaffold-exercises, setup-pre-commit. In repo but NOT in plugin.json.

### Plugin Registry
Two components:
- **`plugin.json`**: the machine-readable registry. Lists all published skills with their paths and descriptions.
- **`link-skills.sh`**: creates symlinks from `~/.claude/skills/<skill-name>/` to the actual skill folders. Excludes `deprecated/` explicitly (commit `494e4b2`).

### Setup Configuration Templates
Not stored in the skills library at rest — generated and written into the **developer's target repo** when `setup-matt-pocock-skills` is invoked. Contents vary by user choices (issue tracker type, domain vocabulary, label strings). Idempotent: will not overwrite existing config.

### Out-of-Scope Registry
🟢 Records rejected enhancement proposals as `.out-of-scope/<concept>.md` files. The `triage` skill reads these using concept-similarity matching (not keyword matching) before triaging a new issue. If a new issue conceptually duplicates a rejected enhancement, the skill flags it.

### Documentation & ADRs
Not executable — informational only. `CLAUDE.md` is the contributor governance document (rules for skill structure, publication, and README maintenance).

---

## Data Flows

| Flow | From | To | Trigger |
|------|------|----|---------| 
| Skill invocation | Claude Code | Active Skill Buckets | Developer slash command |
| Config seeding | Active Skill Buckets (setup skill) | Developer's Target Repo | First run of setup-matt-pocock-skills |
| Registry update | Maintainer | Plugin Registry | New skill published or deprecated |
| Issue creation | Claude Code | Issue Tracker | to-issues or to-prd skill invoked |
| Label application | Claude Code | Issue Tracker | triage skill state transition |
| Out-of-scope entry | Claude Code | Out-of-Scope Registry | triage closes enhancement as wontfix |
