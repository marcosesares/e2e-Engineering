# C4 Context Diagram — skills

> Generated by Reversa Architect on 2026-05-15
> Level 1: System in its environment
> Confidence: 🟢 CONFIRMED

---

## Diagram

```mermaid
C4Context
    title Skills Library — Context Diagram (C4 Level 1)

    Person(developer, "Developer", "Uses skills in their own codebase project. Invokes skills via Claude Code slash commands.")
    Person(maintainer, "Maintainer", "Authors, publishes, and deprecates skills. Manages the skill registry. Drives triage state machine.")
    Person(reporter, "Reporter", "Opens issues against the maintainer's projects. Provides info when requested during triage.")

    System(skills_library, "Matt Pocock Skills Library", "A curated collection of AI agent instruction sets (skills) for engineering and productivity workflows. Skills are Markdown natural-language programs executed by AI agents.")

    System_Ext(claude_code, "Claude Code", "Anthropic's AI coding assistant. Reads and executes SKILL.md files when a user invokes a slash command. Primary runtime for skills.")
    System_Ext(github_issues, "GitHub Issues", "Issue tracker used by to-issues, to-prd, and triage skills. Receives created issues, applied labels, and AI-generated comments.")
    System_Ext(gitlab_issues, "GitLab Issues", "First-class alternative issue tracker. Equivalent backend to GitHub for triage and issue-creation skills.")
    System_Ext(npm_registry, "npm Registry", "Distribution channel via `npx skills@latest`. Serves the published skill set to developers who install without cloning the repo.")
    System_Ext(target_repo, "Developer's Target Repo", "The codebase the developer is working on. Receives setup configuration (triage-labels.md, CONTEXT.md) seeded by setup-matt-pocock-skills.")

    Rel(developer, skills_library, "Installs and invokes skills from", "npx / link-skills.sh")
    Rel(maintainer, skills_library, "Authors, publishes, deprecates skills in")
    Rel(reporter, skills_library, "Triggers triage workflows indirectly via", "issue creation")

    Rel(developer, claude_code, "Invokes slash commands in", "Claude Code CLI / IDE extension")
    Rel(claude_code, skills_library, "Reads SKILL.md files from", "filesystem symlinks")
    Rel(claude_code, github_issues, "Creates issues, applies labels, posts comments to", "GitHub API")
    Rel(claude_code, gitlab_issues, "Creates issues, applies labels, posts comments to", "GitLab API")
    Rel(claude_code, target_repo, "Reads and writes code in", "filesystem")

    Rel(skills_library, npm_registry, "Published to", "npx skills@latest")
    Rel(skills_library, target_repo, "Seeds configuration into", "setup-matt-pocock-skills")
```

---

## Actor Descriptions

| Actor | Type | Role in system |
|-------|------|---------------|
| Developer | Human | End user; installs skills, invokes them during coding sessions |
| Maintainer | Human | Author and curator; defines skill behavior, drives triage state machine |
| Reporter | Human | External; opens issues that trigger triage workflows |
| Claude Code | External System | Primary AI agent runtime; executes skill instructions |
| GitHub Issues | External System | Hard-dependency issue tracker for to-issues, to-prd, triage |
| GitLab Issues | External System | Hard-dependency issue tracker (added in issue #98) |
| npm Registry | External System | Distribution channel for published skills |
| Developer's Target Repo | External System | Receives setup config seeded by setup-matt-pocock-skills |

---

## Key Relationships

🟢 **Claude Code → Skills Library**: symlinks from `~/.claude/skills/` to the skill folders created by `link-skills.sh`. On invocation, Claude Code reads the SKILL.md and supporting files.

🟢 **Skills Library → Target Repo**: `setup-matt-pocock-skills` writes per-repo config files (`triage-labels.md`, `CONTEXT.md` seed) into the developer's target repository, not into the skills library itself.

🟢 **Skills Library → npm Registry**: Published via `npx skills@latest`. Only `engineering/` and `productivity/` skills are published. `misc/`, `deprecated/`, and `personal/` are excluded.

🟡 **Local Markdown**: a third issue tracker backend exists (no external system dependency). The developer describes the workflow as prose; the skill adapts. Not shown separately to keep the diagram clean.
