# C4 Components Diagram — skills

> Generated by Reversa Architect on 2026-05-15
> Level 3: Components inside the Active Skill Buckets container
> Confidence: 🟢 CONFIRMED

---

## Engineering Bucket Components

```mermaid
C4Component
    title Engineering Bucket — Component Diagram (C4 Level 3)

    Container_Boundary(engineering, "engineering/ bucket") {

        Component(diagnose, "diagnose", "SKILL.md + 1 supporting file", "6-phase diagnostic loop: clarify → reproduce → hypothesis → test → confirm → fix. Soft dependency on setup config. Does not touch issue tracker.")

        Component(grill_with_docs, "grill-with-docs", "SKILL.md + LANGUAGE.md", "Conversational requirements elicitation using project docs. One question at a time. Absorbs ubiquitous-language and domain-model (ADR-0011). Codebase-exploration-first.")

        Component(improve_arch, "improve-codebase-architecture", "SKILL.md + LANGUAGE.md", "Iterative architectural deepening via 'seam' identification. Uses deep module / shallow module / seam vocabulary. Soft dependency on setup config.")

        Component(prototype, "prototype", "SKILL.md + SKILL-logic.md + SKILL-ui.md", "Binary-branch prototype router: logic branch (TUI shell + reducer) vs UI branch (variant comparison). Delete-or-absorb lifecycle. UI prototype hidden in production builds.")

        Component(setup_skill, "setup-matt-pocock-skills", "SKILL.md", "Seeds per-repo config: triage-labels.md, CONTEXT.md, docs/adr/ structure. Idempotent. Soft/hard dependency gatekeeper for other engineering skills. disable-model-invocation: true.")

        Component(tdd, "tdd", "SKILL.md + deep-modules.md + vertical-slice.md + tracer-bullet.md + anti-patterns.md", "Red-green-refactor loop. Vertical slices only. Public interfaces only. Never refactor while RED. Soft dependency on setup config.")

        Component(to_issues, "to-issues", "SKILL.md", "Converts PRD/requirements into GitHub/GitLab issues as vertical slices + tracer bullet. Hard dependency on triage-labels.md. Tracker-agnostic via mapping layer.")

        Component(to_prd, "to-prd", "SKILL.md", "Converts feature request or requirements document into a structured PRD. Hard dependency on triage-labels.md. Inputs: freeform text or existing docs.")

        Component(triage, "triage", "SKILL.md + triage-labels.md (per-repo)", "Full 5-state triage machine: needs-triage → needs-info/ready-for-agent/ready-for-human/wontfix. Exactly 1 state role + 1 category role per issue. Agent Brief on ready-for-agent. Out-of-scope on wontfix enhancement.")

        Component(zoom_out, "zoom-out", "SKILL.md", "Architectural perspective shift. Prompts developer to step back from implementation detail. disable-model-invocation: true. Soft dependency.")
    }

    System_Ext(claude_code, "Claude Code", "Executes components")
    System_Ext(issue_tracker, "Issue Tracker", "GitHub / GitLab")

    Rel(claude_code, diagnose, "Invokes")
    Rel(claude_code, grill_with_docs, "Invokes")
    Rel(claude_code, improve_arch, "Invokes")
    Rel(claude_code, prototype, "Invokes")
    Rel(claude_code, setup_skill, "Invokes (first-time setup)")
    Rel(claude_code, tdd, "Invokes")
    Rel(claude_code, to_issues, "Invokes → creates issues in", "issue tracker")
    Rel(claude_code, to_prd, "Invokes → produces PRD")
    Rel(claude_code, triage, "Invokes → applies labels in", "issue tracker")
    Rel(claude_code, zoom_out, "Invokes (context reset)")

    Rel(to_issues, triage, "Issues created may enter triage pipeline")
    Rel(setup_skill, to_issues, "Seeds triage-labels.md consumed by")
    Rel(setup_skill, to_prd, "Seeds triage-labels.md consumed by")
    Rel(setup_skill, triage, "Seeds triage-labels.md consumed by")
    Rel(grill_with_docs, to_prd, "Grilling output feeds into")
```

---

## Productivity Bucket Components

```mermaid
C4Component
    title Productivity Bucket — Component Diagram (C4 Level 3)

    Container_Boundary(productivity, "productivity/ bucket") {

        Component(caveman, "caveman", "SKILL.md", "Token-compression mode. Toggle: INACTIVE → ACTIVE on trigger phrase. ACTIVE persists across all turns. Exception sub-state for safety-critical sections. Code blocks reproduced verbatim.")

        Component(grill_me, "grill-me", "SKILL.md", "Simpler variant of grill-with-docs. No external doc dependency. One question at a time. Open-ended. Codebase-exploration-first.")

        Component(handoff, "handoff", "SKILL.md", "Generates structured handoff document for agent-to-agent or session-to-session context transfer. Not a passthrough (ADR-0005). Sections: current state, next action, blockers, file paths.")

        Component(write_a_skill, "write-a-skill", "SKILL.md", "Scaffolds a new skill following the SKILL.md conventions. Enforces: ≤100 lines, description ≤1024 chars with 'Use when' trigger, bucket placement rules, README + plugin.json update.")
    }

    System_Ext(claude_code, "Claude Code", "Executes components")

    Rel(claude_code, caveman, "Invokes (toggle compression mode)")
    Rel(claude_code, grill_me, "Invokes (requirements interview)")
    Rel(claude_code, handoff, "Invokes (session transition)")
    Rel(claude_code, write_a_skill, "Invokes (skill authoring)")

    Rel(write_a_skill, caveman, "Produces skills following same conventions as")
    Rel(handoff, grill_me, "Handoff context may prime next grilling session")
```

---

## In-Progress Bucket Components

```mermaid
C4Component
    title In-Progress Bucket — Component Diagram (C4 Level 3)

    Container_Boundary(inprogress, "in-progress/ bucket") {

        Component(review, "review", "SKILL.md", "Two-axis code review: correctness (does it work?) × craft (is it well-written?). Spawns parallel sub-agents per file. Produces structured ReviewReport. No graduation criteria defined.")

        Component(writing_beats, "writing-beats", "SKILL.md", "Beat-by-beat article writing. Journey state machine: AWAITING_PATH → WRITING_BEAT → OFFERING_NEXT → ENDED. Re-reads file from disk before every write. User edits between turns are authoritative.")

        Component(writing_fragments, "writing-fragments", "SKILL.md", "Fragment accumulator. Captures from first user message. Interview loop surfaces fragments. Re-reads file before every write. Pile intentionally incomplete at end.")

        Component(writing_shape, "writing-shape", "SKILL.md", "Article shaping from raw material. Raw material file is read-only — never edited. Produces shaped article in separate target file.")
    }

    System_Ext(claude_code, "Claude Code", "Executes components")

    Rel(claude_code, review, "Invokes (code review)")
    Rel(claude_code, writing_beats, "Invokes (article writing)")
    Rel(claude_code, writing_fragments, "Invokes (idea capture)")
    Rel(claude_code, writing_shape, "Invokes (article shaping)")

    Rel(writing_fragments, writing_shape, "Fragment pile feeds into")
    Rel(writing_shape, writing_beats, "Shaped article may be developed further with")
```

---

## Key Component Interfaces

| Component | Input | Output | Side Effects |
|-----------|-------|--------|--------------|
| `triage` | GitHub/GitLab issue URL or issue content | Labels applied, comments posted | Writes .out-of-scope/ on wontfix |
| `to-issues` | PRD or requirements text | GitHub/GitLab issues created | Issues created in tracker |
| `to-prd` | Feature request / requirements | PRD Markdown document | None (no external writes) |
| `grill-with-docs` | User intent + project docs | Structured requirements | None (conversation only) |
| `tdd` | Feature spec | Test + implementation files | Modifies target repo files |
| `diagnose` | Bug description | Root cause analysis | None by default |
| `prototype` | Design question | TUI shell (logic) or UI variants | Creates throwaway files |
| `setup-matt-pocock-skills` | User preferences | Config files | Writes to target repo |
| `caveman` | Trigger phrase | Compressed responses | Changes agent output style |
| `handoff` | Current session state | Handoff.md document | Creates file |
| `review` | File paths | ReviewReport | None |
| `writing-fragments` | User messages | Fragment accumulation file | Writes/appends to file |
| `writing-beats` | Article path | Beat written to article | Appends to target file |
| `writing-shape` | Raw material + shaped target | Shaped article | Writes to target file |
