# Flowcharts — misc module

> Generated by Reversa Archaeologist on 2026-05-15 | doc_level: detalhado

---

## git-guardrails-claude-code — hook setup

```mermaid
flowchart TD
    START([/git-guardrails-claude-code]) --> SCOPE{Ask scope}
    SCOPE -- project --> PROJECT[Target: .claude/settings.json\nScript: .claude/hooks/block-dangerous-git.sh]
    SCOPE -- global --> GLOBAL[Target: ~/.claude/settings.json\nScript: ~/.claude/hooks/block-dangerous-git.sh]

    PROJECT & GLOBAL --> COPY[Copy bundled script to target location]
    COPY --> CHMOD[chmod +x]
    CHMOD --> MERGE[Merge hook into settings.json hooks.PreToolUse array\n RULE: non-destructive — preserve other settings]
    MERGE --> CUSTOM{Ask about customization?}
    CUSTOM -- yes --> EDIT[Edit script: add/remove blocked patterns]
    CUSTOM -- no --> VERIFY
    EDIT --> VERIFY

    subgraph VERIFY["Verify"]
        V1["echo '{tool_input:{command:git push origin main}}' | <script>"]
        V2{Exit 2 + BLOCKED to stderr?}
        V3[Fix script]
        V1 --> V2
        V2 -- no --> V3 --> V1
        V2 -- yes --> DONE
    end

    DONE([Done])

    subgraph BLOCKED_CMDS["Blocked patterns"]
        B1[git push - all variants]
        B2[git reset --hard]
        B3[git clean -f / -fd]
        B4[git branch -D]
        B5[git checkout .]
        B6[git restore .]
    end
```

---

## migrate-to-shoehorn — migration workflow

```mermaid
flowchart TD
    START([/migrate-to-shoehorn]) --> GATHER[Gather requirements]
    GATHER --> Q1[Which test files have as assertions?]
    GATHER --> Q2[Large objects with few needed props?]
    GATHER --> Q3[Intentionally wrong data for error testing?]

    Q1 & Q2 & Q3 --> INSTALL[npm i @total-typescript/shoehorn]
    INSTALL --> FIND["grep -r ' as [A-Z]' --include='*.test.ts' --include='*.spec.ts'"]
    FIND --> MIGRATE[For each match]

    MIGRATE --> PATTERN{Pattern type}
    PATTERN -- "obj as Type" --> FP[Replace with fromPartial]
    PATTERN -- "obj as unknown as Type" --> FA[Replace with fromAny]

    FP & FA --> IMPORTS[Add imports from @total-typescript/shoehorn]
    IMPORTS --> TYPECHECK[Run type check]
    TYPECHECK --> TC_RESULT{Pass?}
    TC_RESULT -- no --> FIX[Fix remaining type errors]
    FIX --> TYPECHECK
    TC_RESULT -- yes --> DONE([Done])

    NOTE["⚠️ shoehorn = TEST CODE ONLY\nnever use in production"]
```

---

## scaffold-exercises — directory creation + lint loop

```mermaid
flowchart TD
    START([/scaffold-exercises plan]) --> PARSE[Parse plan: sections + exercises + variants]
    
    PARSE --> CREATE_DIRS[mkdir -p for each path]
    
    subgraph NAMING["Naming rules"]
        N1["Sections: exercises/XX-section-name/"]
        N2["Exercises: XX.YY-exercise-name/"]
        N3["Variants: problem/ | solution/ | explainer/"]
    end

    CREATE_DIRS --> STUBS[Create stub readmes per variant]
    STUBS --> README_CONTENT["readme.md: # Title\n\nDescription (non-empty)"]
    
    README_CONTENT --> LINT[pnpm ai-hero-cli internal lint]
    LINT --> LINT_RESULT{Pass?}
    LINT_RESULT -- yes --> COMMIT[git commit]
    LINT_RESULT -- no --> FIX[Fix errors]

    subgraph FIX_SUB["Common fixes"]
        F1[Non-empty readmes]
        F2[No broken links]
        F3[Remove .gitkeep]
        F4[Remove speaker-notes.md]
        F5[No pnpm run exercise in readmes]
        F6[Add main.ts if subfolder has code]
    end

    FIX --> LINT

    RENAME[Rename/move exercises] --> GITMV["git mv (NOT mv) — preserves history"]
    GITMV --> LINT

    COMMIT --> DONE([Done])
```

---

## setup-pre-commit — detection + installation

```mermaid
flowchart TD
    START([/setup-pre-commit]) --> DETECT{Detect package manager}
    DETECT -- package-lock.json --> NPM[npm]
    DETECT -- pnpm-lock.yaml --> PNPM[pnpm]
    DETECT -- yarn.lock --> YARN[yarn]
    DETECT -- bun.lockb --> BUN[bun]
    DETECT -- none --> NPM

    NPM & PNPM & YARN & BUN --> INSTALL[Install devDeps: husky lint-staged prettier]
    INSTALL --> HUSKY_INIT[npx husky init]
    HUSKY_INIT --> PRECOMMIT[Create .husky/pre-commit]

    subgraph PRECOMMIT_CONTENT["pre-commit content"]
        PC1[npx lint-staged]
        PC2{typecheck script exists?}
        PC3[pm run typecheck]
        PC2 -- yes --> PC3
        PC4{test script exists?}
        PC5[pm run test]
        PC4 -- yes --> PC5
        PC2 -- no --> SKIP_TC[omit + tell user]
        PC4 -- no --> SKIP_T[omit + tell user]
    end

    PRECOMMIT --> LINTSTAGED[Create .lintstagedrc]
    LINTSTAGED --> LINTSTAGED_CONTENT["{\"*\": \"prettier --ignore-unknown --write\"}"]

    LINTSTAGED_CONTENT --> PRETTIER_CHK{Prettier config exists?}
    PRETTIER_CHK -- yes --> SKIP_P[Skip - idempotent]
    PRETTIER_CHK -- no --> CREATE_P[Create .prettierrc with defaults]

    SKIP_P & CREATE_P --> VERIFY[Verify checklist]
    
    subgraph VERIFY_SUB["Verify"]
        V1[.husky/pre-commit exists + executable]
        V2[.lintstagedrc exists]
        V3[package.json prepare = husky]
        V4[prettier config exists]
        V5[npx lint-staged runs without error]
    end

    VERIFY --> COMMIT[Stage + commit all changes]
    COMMIT --> SMOKE[Commit runs through new hooks = smoke test]
    SMOKE --> DONE([Done])
```
