# Legacy Mapping — CI/CD Pipelines

> Maps every file composing the CI/CD Pipelines module back to its on-disk path and the role it plays.
> Generated by Reversa Archaeologist on 2026-05-23.

## Pipeline files (repository root)

| File | Lines | Type | Trigger | Output |
|---|---:|---|---|---|
| `publish-claude-skills.yml` | 63 | Azure DevOps Pipelines YAML | push to `master` touching `claude-skills/*` | `published-skills` branch (force-pushed) |
| `publish-testkit.yml` | 87 | Azure DevOps Pipelines YAML | push to `master` touching `dotnet/BeckTech.QA.TestKit/**` | `.nupkg` packages pushed to `DESTINI-Web` Azure Artifacts feed |

## Step inventory — `publish-claude-skills.yml`

| Lines | Step | Function |
|---:|---|---|
| 11–19 | `trigger` | Branch + path filter; `pr: none` |
| 21–24 | `resources` | `repository: self` |
| 26–27 | `pool` | `windows-latest` |
| 30–32 | `checkout: self` | `fetchDepth: 0`, `persistCredentials: true` |
| 34–63 | inline `powershell` | git config → branch reset → `subtree split` → force-push |

## Step inventory — `publish-testkit.yml`

| Lines | Step | Function |
|---:|---|---|
| 13–19 | `trigger` | Branch + path filter (`dotnet/BeckTech.QA.TestKit/**`); `pr: none` |
| 23–26 | `parameters` | `azureArtifactsPath` (default: DESTINI-Web NuGet feed URL) |
| 28–30 | `variables` | `solution`, `buildConfiguration: Release` |
| 32–33 | `pool` | `windows-latest` |
| 36–37 | `checkout: self` | `fetchDepth: 1` |
| 39–43 | `UseDotNet@2` | sdk 9.0.x |
| 45–46 | `script: dotnet restore` | restore solution |
| 50–51 | `script: dotnet build` | build Release; emits `.nupkg` via `GeneratePackageOnBuild` |
| 53–54 | `script: dotnet test` | trx + console loggers, `--no-build` |
| 56–62 | `PublishTestResults@2` | VSTest format, merge results, `succeededOrFailed()` |
| 64–70 | `CopyFiles@2` | gather `**/bin/Release/*.nupkg` → staging, `flattenFolders: true` |
| 72–73 | `NuGetAuthenticate@1` | wires Azure Artifacts credentials |
| 81–87 | inline `pwsh` | enumerate nupkgs → `dotnet nuget push --skip-duplicate` |

## Source-of-truth coupling

| Pipeline | Reads from | Writes to | Functional sibling |
|---|---|---|---|
| `publish-claude-skills.yml` | `claude-skills/` tree at HEAD of `master` | `published-skills` branch | `claude-skills/setup/publish-skills.ps1` (manual path) |
| `publish-testkit.yml` | `dotnet/BeckTech.QA.TestKit/**`, `<Version>` in each `src/*.csproj` | `DESTINI-Web` Azure Artifacts feed | none (no local manual NuGet publish script) |

## Gaps

- 🔴 `dotnet/BeckTech.QA.TestKit/` is **currently deleted in the working tree** (entire directory removed in `git status`). The pipeline file is still tracked and unmodified. Confirm: is the pipeline still active, or has TestKit moved to another repo?
- 🔴 No service-connection name is declared in `publish-testkit.yml`; `NuGetAuthenticate@1` uses the project's default Azure Artifacts service connection. Identity of publisher is implicit.
- 🟡 ADO path filter `claude-skills/*` (single-level glob) appears to match deeper paths in practice (confirmed via commit history). Re-confirm in pipeline run logs if behaviour changes.

## Recent change history

| Commit | PR | Touch |
|---|---|---|
| `bc5df85` | 14547 | Fix `publish-testkit` NuGet push: resolve nupkg wildcard in pwsh |
| `4a67068` | 14513 | Extract `BeckTech.QA.TestKit` + `.Playwright` as NuGet packages (introduces `publish-testkit.yml`) |
| `71a0d10` | 14285 | Add shared QA automation skills (subtree-published) — introduces `publish-claude-skills.yml` |
