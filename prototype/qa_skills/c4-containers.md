# C4 Diagram — Level 2: Containers

> Generated by Reversa Architect · 2026-05-23
> Confidence: 🟢 CONFIRMADO | 🟡 INFERIDO | 🔴 LACUNA

---

## Container Diagram

```mermaid
C4Container
    title Container Diagram — BeckTech.QA.Tools

    Person(maintainer, "Maintainer", "Develops skills")
    Person(qa_engineer, "QA Engineer", "Invokes skills in consuming repos")

    System_Ext(jira, "Jira / Atlassian", "Work item tracking")
    System_Ext(ado, "Azure DevOps", "Test management + pipelines")
    System_Ext(github_actions, "GitHub Actions", "CI/CD runner")
    System_Ext(claude_api, "Claude API", "AI inference (Sonnet)")
    System_Ext(playwright_mcp, "Playwright MCP", "Local browser automation")

    Enterprise_Boundary(becktech, "Beck Technology") {

        System_Boundary(qa_tools_repo, "BeckTech.QA.Tools (Producer Repo)") {
            Container(skill_runtime, "Skills Layer", "Markdown (SKILL.md)", "19 composable Claude Code skills. Stateless, entry-point-driven workflows for test planning, bug fixes, and ADO management.")
            Container(agent_layer, "Agents Layer", "Markdown + Sonnet", "2 AI agents: qa-investigate (read-only) and qa-implement (write). Spawned by orchestration skills.")
            Container(script_layer, "Scripts Layer", "PowerShell 7+", "22 .ps1 scripts. Leaf nodes. Each wraps a single REST or MCP surface. Returns Standard Output Envelope.")
            Container(dist_infra, "Distribution Infrastructure", "PowerShell + git", "sync-shared-skills.ps1, guard-shared-skills.ps1, check-updates.ps1, publish-skills.ps1, SHARED_MANIFEST.")
            Container(ci_pipelines, "CI/CD Pipelines", "GitHub Actions YAML + ADO YAML", "publish-claude-skills.yml (skills) and publish-testkit.yml (NuGet).")
        }

        Container(published_branch, "published-skills branch", "git (GitHub)", "Derived projection of claude-skills/. Force-pushed on each publish. Read-only for consumers.")

        System_Boundary(consumer_repo, "Consumer Repo (e.g., BeckTech.QA.Estimator)") {
            Container(synced_skills, "Synced Skills (.claude/)", "Markdown + PowerShell", "Local copy of shared skills pulled via git subtree. Guarded against edits.")
            Container(repo_context, "Repo Context (repo-context/SKILL.md)", "Markdown", "Consumer-owned skill. Holds project-specific constants: ADO field defaults, QA Functional Areas.")
            Container(claude_session, "Claude Code Session", "Claude Code CLI", "Runtime environment. Loads skills, invokes agents, executes hooks.")
        }

        Container(nuget_feed, "DESTINI-Web NuGet Feed", "Azure Artifacts", "Hosts BeckTech.QA.TestKit and BeckTech.QA.TestKit.Playwright packages.")
    }

    Rel(maintainer, skill_runtime, "Develops skills", "local editor")
    Rel(maintainer, dist_infra, "Triggers publish", "publish-skills.ps1")
    Rel(qa_engineer, claude_session, "Invokes /commands", "Claude Code CLI")

    Rel(skill_runtime, agent_layer, "Spawns agents", "Agent tool call")
    Rel(skill_runtime, script_layer, "Calls scripts", "Bash tool + stdin/stdout")
    Rel(skill_runtime, synced_skills, "Composed from", "Skill chain")

    Rel(agent_layer, claude_api, "Inference requests", "Sonnet model")
    Rel(agent_layer, playwright_mcp, "Browser automation", "MCP tool calls")

    Rel(script_layer, jira, "REST API calls", "HTTPS + JSON")
    Rel(script_layer, ado, "MCP + ADO REST", "mcp__ado_* tools")

    Rel(dist_infra, published_branch, "Force-push on publish", "git subtree split")
    Rel(ci_pipelines, published_branch, "Rebuilds on PR merge", "git subtree split + force-push")
    Rel(ci_pipelines, nuget_feed, "Publishes NuGet packages", "dotnet nuget push")

    Rel(synced_skills, published_branch, "Pulls updates", "git subtree pull --squash")
    Rel(claude_session, synced_skills, "Loads and executes", "SKILL.md parsing")
    Rel(claude_session, repo_context, "Loads project constants", "frontmatter skills: list")

    Rel(github_actions, ci_pipelines, "Executes on PR merge", "workflow trigger")
```

---

## Container Descriptions

| Container | Technology | Responsibility | Confidence |
|-----------|------------|----------------|------------|
| **Skills Layer** | Markdown (SKILL.md) | 19 stateless workflows; composable via skill chain | 🟢 |
| **Agents Layer** | Markdown + Sonnet | 2 AI workers with declared tool permissions | 🟢 |
| **Scripts Layer** | PowerShell 7+ | 22 REST/MCP adapters; Standard Output Envelope contract | 🟢 |
| **Distribution Infrastructure** | PowerShell + git | Sync, guard, update-check, publish orchestration | 🟢 |
| **CI/CD Pipelines** | GitHub Actions + ADO YAML | Automates publish-skills and TestKit NuGet push | 🟢 |
| **published-skills branch** | git | Derived projection; history disposable; force-pushed | 🟢 |
| **Synced Skills** | Markdown + PowerShell | Consumer-local copy; guarded against edits by hook | 🟢 |
| **Repo Context** | Markdown | Consumer-owned constants (not synced, not guarded) | 🟢 |
| **Claude Code Session** | Claude Code CLI | Runtime; loads skills, runs agents, fires hooks | 🟢 |
| **DESTINI-Web NuGet Feed** | Azure Artifacts | Package hosting for TestKit libraries | 🟢 |

---

## Communication Protocols

| From → To | Protocol | Sync / Async | Confidence |
|-----------|----------|--------------|------------|
| Skills → Scripts | Bash tool (stdin JSON / stdout) | Sync | 🟢 |
| Scripts → Jira | HTTPS REST (`application/json`) | Sync | 🟢 |
| Scripts → ADO | MCP tool calls (mcp__ado_*) | Sync | 🟢 |
| Agents → Claude API | Model inference API | Sync | 🟢 |
| CI → published-skills | `git push --force` | Async (event-triggered) | 🟢 |
| Consumer → published-skills | `git subtree pull --squash` | Sync (on demand) | 🟢 |
| CI → NuGet Feed | `dotnet nuget push` | Async (event-triggered) | 🟢 |
