# Gap Register — BeckTech.QA.Tools

> Generated by Reversa Reviewer · 2026-05-23 · doc_level = **detalhado**
> **Updated after self-answer pass 2026-05-23:** 17 of 20 questions resolved from direct code inspection; 3 remain open (Q-3, Q-14, Q-17). Spec corrections applied in-place; see `confidence-report.md` Reclassifications for the audit trail.
> Severity scale: **Crítico** · **Moderado** · **Cosmético**.

---

## 🔴 Crítico — Remaining (1)

### C-01 — NuGetAuthenticate service connection name unknown (Q-3)

`publish-testkit.yml` uses `NuGetAuthenticate@1` with no `nuGetServiceConnections` input. The name lives in ADO project settings, not in this repo. Manual answer needed.

**Effect:** Any consumer reproducing the pipeline in another project must guess the connection name; auditing publisher identity for compliance is blocked.

**Action:** Marcos (or ADO admin) provides connection name; document in `cicd/contracts.md` NuGet Feed Contract.

---

## 🟢 Crítico — Resolved this pass

### ~~C-02~~ — Factual errors in `traceability/code-spec-matrix.md` (Q-9, Q-10, Q-11) ✅

Regenerated 2026-05-23 against live tree. Fabricated skills/scripts removed; `qa-evaluate-regression-rubric` added; real flat layout in `claude-skills/scripts/` and `claude-skills/setup-skills/` documented.

### ~~C-03~~ — Pipeline engine misidentified (Q-1) ✅

`architecture.md §4.4 / §5.3 / data-flow §5.3` and `inventory.md §1` corrected to **Azure DevOps Pipelines** with `push to master` trigger and `pr: none`.

### ~~C-04~~ — TestKit pipeline producer status unclear (Q-2) ✅

Deletion is **local working-tree only** (no removal commit). Pipeline remains active. `architecture.md TD-07` clarified; `domain.md G-04` reworded.

### ~~C-05~~ — Picklist literals unverified for regression dispositions (Q-4, Q-5) ✅

Picklist values for `Custom.RegressionCoverageEvaluation` and `Custom.ManualReason` were already documented in `claude-skills/skills/create-ado-test-cases/references/custom-fields.md`. `domain.md G-01` and `G-02` retired.

---

## 🟡 Moderado — Remaining (2)

### M-01 — Performance not characterized (Q-14)

No measured benchmarks for `apply-test-plan` at 50 / 100 / 200 TCs. Theoretical lower bound is ~400 API calls for 100 TCs (3 calls × 100 + 100 verifications). ADO API soft caps documented at Microsoft side (~200 req/sec/user). Practical limit unknown.

**Action:** Record as "performance not characterized" in `scripts/requirements.md NFR-Performance`. Add a benchmark task to backlog. **Will be applied in this pass as a doc note.**

### M-02 — Hook test coverage backlog (Q-17)

No automated tests for `guard-shared-skills.ps1`, `sync-shared-skills.ps1`, `check-updates.ps1`. Confirmed via `find . -name "*.Tests.ps1"` returning empty. Marcos to confirm priority.

**Action:** Pester suite is a recognized debt (TD-02, G-06). No spec change beyond keeping the debt visible.

---

## 🟢 Moderado — Resolved this pass

### ~~M-03~~ — Setup-step count contradiction (Q-7) ✅

`claude-skills/requirements.md RF-10` rewritten to clarify 7 per-user + 2 project-scope = 9 leaf skills.
`claude-skills/tasks.md T-16`: 8 → **9**, broken out into per-user and project-scope.
`setup/tasks.md T-05`: 8 → **9** with explicit breakdown.

### ~~M-04~~ — Architecture script bucketing wrong (Q-8) ✅

`architecture.md §3 layer map` and `§4.3 Script Layer`: rebucketed to **Jira:7 · ADO:14 · Generation:1** matching `scripts/legacy-mapping.md`. Note added that setup/distribution scripts live in the Setup unit, not Scripts.

### ~~M-05~~ — `qa-evaluate-regression-rubric` un-specified (Q-9) ✅

Added:
- `claude-skills/requirements.md` RF-18
- `claude-skills/design.md` Internal Workflows table row
- `claude-skills/tasks.md` T-22
- `traceability/code-spec-matrix.md` Claude Skills table row

### ~~M-06~~ — Inventory.md drift (Q-19) ✅

`inventory.md` corrected: total **380** (was ~399), Jira-script count **7** (was 4), Azure DevOps Pipelines tag (was GitHub Actions), 9 setup-skills under `setup-skills/` (was 8 under `skills/`).

### ~~M-07~~ — Exception/retry/rollback policy not pinned (Q-12, Q-13) ✅

Confirmed via direct inspection of `create-ado-test-cases.ps1`:
- **No retry, no backoff** — single try/catch in `Invoke-AdoApi` (lines 55-81).
- **No rollback** — failed TCs accumulate in `failures[]` with `partial: true` flag when steps/state PATCH fails post-create; created TCs remain in ADO.
- Same pattern across all 22 scripts.

Reclassified `scripts/edge-cases.md Case 1`, `Case 2`, `claude-skills/edge-cases.md Case 2` from 🔴/🟡 to 🟢 (behavior documented).

### ~~M-08~~ — Concurrency assumption undocumented (Q-15) ✅

`architecture.md §2` Concurrency row added. `claude-skills/design.md` Concurrency Assumption section added: single-user single-session per repo; parallel `/work-on` on different keys is safe; parallel `/apply-test-plan` against the same repo is unsafe.

### ~~M-09~~ — qa-investigate read-only enforcement unclear (Q-20) ✅

`agents/design.md` now documents the two-layer model: tool whitelist (no Write/Edit) + prompt policy + Bash escape hatch. `agents/edge-cases.md Case 5` flags the wording defect in `qa-investigate.md` (cites a Write tool not granted in frontmatter; actual mechanism is Bash redirect).

### ~~M-10~~ — Approval phrase patterns un-pinned (Q-16) ✅

`claude-skills/design.md` Approval Gate Phrase Patterns section added with exact phrases from each SKILL.md plus the model-discretionary rule.

### ~~M-11~~ — notify.ps1 surface (Q-18) ✅

`architecture.md TD-08` fully described (Windows-only balloon, hardcoded title/body, no PII/network, 13 LOC). `domain.md` glossary unchanged; no further spec needed.

### ~~M-12~~ — `download-attachment.ps1` fictional row (Q-11) ✅

Removed from `traceability/spec-impact-matrix.md`.

---

## Cosmético

### K-01 — Numbering gap `BR-JI-04` in `claude-skills/requirements.md` ✅ Resolved

BR-JI-04 (feature plan writeback) added to `claude-skills/requirements.md` in this pass, referencing `BR-AP-03` for context. Numbering is now contiguous.

### K-02 — ADR-001 cross-link

`claude-skills/decisions.md ADR-001` cites BR-SD-01/02 defined in `domain.md §2.6`. Both files coherent; cross-link could be made explicit but the link is already implicit. **No action.**

### K-03 — Missing `cicd/questions.md`

Other units have one; cicd does not. Open `cicd` 🔴/🟡 items are already aggregated in this `gaps.md` and `questions.md`. **No action this pass.**

### K-04 — `inventory.md` `Last Mapped` date

Bumped to "Reviewed 2026-05-23" in header. No further action.

---

## Summary

| State | Before review | After review | After self-answer (2026-05-23) |
|-------|--------------:|-------------:|------------------------------:|
| 🔴 Crítico | 0 (Writer baseline) | 4 | **1** |
| 🟡 Moderado | 0 | 11 | **2** |
| Cosmético | 0 | 3 | **0** (1 active, 3 closed) |

**Outstanding for human/external input:** Q-3 only (NuGetAuthenticate service connection name). Q-14 and Q-17 are recognized backlog/debt; not blocking.
