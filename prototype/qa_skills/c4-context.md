# C4 Diagram — Level 1: System Context

> Generated by Reversa Architect · 2026-05-23
> Confidence: 🟢 CONFIRMADO | 🟡 INFERIDO | 🔴 LACUNA

---

## Context Diagram

```mermaid
C4Context
    title System Context — BeckTech.QA.Tools

    Person(maintainer, "Maintainer", "BeckTech QA team member. Develops and publishes skills, updates pipelines.")
    Person(qa_engineer, "QA Engineer", "Downstream consumer. Invokes skills in consuming repos to plan tests, manage ADO cases, fix bugs.")

    Enterprise_Boundary(becktech, "Beck Technology") {
        System(qa_tools, "BeckTech.QA.Tools", "QA automation framework. Distributes Claude Code skills, PowerShell scripts, and agents for test planning, case management, and Jira/ADO integration.")
    }

    System_Ext(jira, "Jira / Atlassian", "Work item tracking. Stores QA Stories, Bugs, Spikes, custom fields (TestPlan ID, TestSuite ID). Cloud-hosted.")
    System_Ext(ado, "Azure DevOps", "Test plans, test cases, work items, repos, and pipelines. Hosts DESTINI-Web NuGet feed via Azure Artifacts.")
    System_Ext(github, "GitHub", "Source control. Hosts BeckTech.QA.Tools and published-skills branch. Runs GitHub Actions CI/CD.")
    System_Ext(playwright, "Playwright", "Local browser automation. Chromium/Firefox/WebKit. Used for web UI test execution in consuming repos.")
    System_Ext(claude_api, "Claude API", "AI inference (Sonnet model). Executes agents qa-investigate and qa-implement during skill workflows.")

    Rel(maintainer, qa_tools, "Develops skills, triggers publish", "local + git push")
    Rel(qa_engineer, qa_tools, "Invokes /commands in consuming repos", "Claude Code CLI")

    Rel(qa_tools, jira, "Reads issues, writes comments/fields/transitions", "Atlassian MCP + REST API")
    Rel(qa_tools, ado, "Manages test cases, suites, plans, work items, PRs", "ADO MCP")
    Rel(qa_tools, github, "Publishes published-skills branch on PR merge", "git subtree + Actions")
    Rel(qa_tools, playwright, "Browser automation for UI test execution", "Playwright MCP")
    Rel(qa_tools, claude_api, "Agent execution (qa-investigate, qa-implement)", "Sonnet model API")

    UpdateRelStyle(maintainer, qa_tools, $textColor="gray", $lineColor="gray")
    UpdateRelStyle(qa_engineer, qa_tools, $textColor="gray", $lineColor="gray")
```

---

## Actors

| Actor | Type | Description | Confidence |
|-------|------|-------------|------------|
| **Maintainer** | Human | BeckTech QA team member with write access to `BeckTech.QA.Tools`. Develops skills, updates SHARED_MANIFEST, triggers CI publish. | 🟢 |
| **QA Engineer** | Human | Works in a consuming repo (e.g., `BeckTech.QA.Estimator`). Invokes skills via `/work-on`, `/apply-test-plan`, etc. Cannot edit synced files. | 🟢 |

---

## Systems

| System | Role | Protocols | Confidence |
|--------|------|-----------|------------|
| **BeckTech.QA.Tools** | Producer framework | git, Claude Code | 🟢 |
| **Jira / Atlassian** | Work item source of truth | MCP + REST API | 🟢 |
| **Azure DevOps** | Test management + packaging | MCP + Azure Artifacts | 🟢 |
| **GitHub** | Source control + CI | git + GitHub Actions webhooks | 🟢 |
| **Playwright** | UI test automation | Playwright MCP (local) | 🟢 |
| **Claude API** | AI inference for agents | Model API (Sonnet) | 🟢 |

---

## Key Boundaries

- **BeckTech.QA.Tools** produces artifacts; it does **not** own state. All test case data lives in ADO; all issue data lives in Jira.
- The `published-skills` branch is the **only output channel** for skill distribution. Consumer repos pull from it.
- NuGet packages (TestKit) are a **separate output channel** via Azure Artifacts — independent from skill distribution.
- Claude API is an **implicit runtime dependency** — skills do not call it directly; Claude Code session uses it transparently.
