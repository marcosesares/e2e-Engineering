# Questions for Validation — superpowers

> Generated by Reviewer on 2026-05-17.
> Answer each question and notify the Reviewer when finished.

---

## Question 1

**Context:** `caveman` and `caveman-stats` behavior depends on hook-based injection that is referenced but not implemented in the visible skill Markdown.
**Affected spec:** `_reversa_sdd/caveman/design.md`, `_reversa_sdd/caveman/edge-cases.md`
**Question:** Is there an external hook or runtime layer that injects caveman auto-activation and token statistics, or should those behaviors be documented as unsupported by this repository?
**Impact:** If an external hook exists, the specs should reference its contract. If not, caveman auto-activation and stats must remain 🔴 or be downgraded to non-implemented behavior.

✅ Responded and processed by Reviewer.

**Answer:** No external hook/runtime layer is present in this repository for `caveman` auto-activation or `caveman-stats` token accounting. The visible hook set only installs session-start context; the referenced `hooks/caveman-stats.js` and `hooks/caveman-mode-tracker.js` are not present.

Document auto-activation as a host skill-trigger behavior when the host supports trigger matching, not as a repository-implemented hook. Document `caveman-stats` as 🔴 unsupported/incomplete in this repository until the hook files or an equivalent host contract are added. The intended contract can remain described, but must be marked as not implemented here.

---

## Question 2

**Context:** `cavecrew` delegates to reviewer/explorer style agents, but long-session context budget and user tolerance for compressed output are not defined in code.
**Affected spec:** `_reversa_sdd/cavecrew/design.md`, `_reversa_sdd/cavecrew/tasks.md`
**Question:** Should Cavecrew enforce a maximum number of delegations or context threshold before pausing, and who is the expected audience for compressed cave-style output?
**Impact:** The answer determines whether a hard guard, soft recommendation, or user-configurable setting belongs in the reimplementation.

✅ Responded and processed by Reviewer.

**Answer:** Cavecrew should use a soft recommendation, not a hard delegation cap. The repository defines when cavecrew is appropriate and warns that output can be cryptic, but it does not define a numeric context threshold or maximum delegation count.

The expected direct audience for compressed cave-style output is the main/controller agent, not an end user. If a human will read the result directly, the controller should paraphrase it into normal prose. A reimplementation can expose an optional user-configurable delegation/context budget, but the default should be advisory rather than blocking.

---

## Question 3

**Context:** `dispatching-parallel-agents` mentions timeout and cross-agent communication limits, but exact timeout configuration is not defined.
**Affected spec:** `_reversa_sdd/dispatching-parallel-agents/design.md`, `_reversa_sdd/dispatching-parallel-agents/tasks.md`
**Question:** Should parallel-agent waits use a fixed timeout, a configurable timeout per task, or the host engine default?
**Impact:** This affects orchestration reliability and the task state model for incomplete subagents.

✅ Responded and processed by Reviewer.

**Answer:** Use the host engine default wait behavior by default, with an optional per-task timeout override when the host exposes one. The skill does not define a fixed timeout value, so a hardcoded timeout would be invented behavior.

For incomplete subagents, the task state model should record `pending`, `timed_out`, or `returned_partial` based on the host result. The controller should review partial results, avoid assuming completion, and either continue waiting, re-dispatch narrowly, or proceed without that result if the task is non-blocking.

---

## Question 4

**Context:** `executing-plans` assumes task execution can resume after partial failure, but rollback versus continue behavior is not fully specified.
**Affected spec:** `_reversa_sdd/executing-plans/design.md`, `_reversa_sdd/executing-plans/tasks.md`
**Question:** When a plan task partially succeeds and verification fails, should the agent roll back its own edits, continue from the partial state, or ask the user case by case?
**Impact:** This determines recovery behavior and whether tasks need idempotency metadata.

✅ Responded and processed by Reviewer.

**Answer:** Continue from the partial state after diagnosing the failed verification. Do not automatically roll back edits, because the skills emphasize root-cause investigation, targeted fixes, and preserving work unless the user explicitly chooses discard/destructive cleanup.

Ask the user case by case only when recovery would require destructive rollback, broad rewrite, or the plan is materially wrong. Reimplementation tasks should be written to be idempotent enough to resume from partial completion, with verification evidence deciding the next action.

---

## Question 5

**Context:** `finishing-a-development-branch` references branch completion and merge readiness, but merge strategy is not fixed.
**Affected spec:** `_reversa_sdd/finishing-a-development-branch/design.md`, `_reversa_sdd/finishing-a-development-branch/tasks.md`
**Question:** Should the default finishing strategy assume squash merge, merge commit, rebase, or leave merge strategy entirely outside the skill?
**Impact:** This affects generated branch handoff instructions and commit-preservation expectations.

✅ Responded and processed by Reviewer.

**Answer:** Leave merge strategy outside the skill by default. The finishing skill presents workflow options: merge locally, push/create PR, keep branch, or discard. It does not mandate squash, rebase, or merge commits.

For local merge, use the repository/host default `git merge` behavior unless the user or project policy specifies otherwise. For PRs, the platform or maintainer chooses squash/rebase/merge at merge time.

---

## Question 6

**Context:** `requesting-code-review` and `subagent-driven-development` rely on subagent APIs, reviewer models, and isolation guarantees that vary by host engine.
**Affected spec:** `_reversa_sdd/requesting-code-review/design.md`, `_reversa_sdd/subagent-driven-development/design.md`, `_reversa_sdd/subagent-driven-development/tasks.md`
**Question:** Which host engines should be treated as first-class targets for subagent dispatch semantics: Claude Code only, Codex only, or all listed engines?
**Impact:** This determines whether the reimplementation needs a cross-engine adapter or a single-engine contract.

✅ Responded and processed by Reviewer.

**Answer:** Treat all listed engines as first-class targets for skill loading and workflow semantics, but use a capability adapter for subagent dispatch. Claude Code and Codex-style environments can be first-class for rich subagent semantics where those APIs are available; other engines must either map to their native agent mechanism or degrade gracefully.

The reimplementation should not assume a single-engine contract. It should define shared concepts such as `dispatch_agent`, `review_agent`, isolated context, task ownership, and result status, then map those concepts per host. Where an engine lacks subagents, the workflow should fall back to inline execution or documented manual review.

---

## Question 7

**Context:** `systematic-debugging`, `test-driven-development`, and `verification-before-completion` require evidence, but no persisted evidence schema is defined.
**Affected spec:** `_reversa_sdd/systematic-debugging/design.md`, `_reversa_sdd/test-driven-development/design.md`, `_reversa_sdd/verification-before-completion/design.md`
**Question:** Should debugging, TDD, and verification evidence be persisted to files, held only in conversation state, or delegated to the host engine's task/checkpoint mechanism?
**Impact:** This affects resumability, auditability, and whether reimplementation needs durable evidence logs.

✅ Responded and processed by Reviewer.

**Answer:** Persist verification evidence to files in the reimplementation, with conversation state used only as the live working context. The current skills require evidence, root-cause notes, red/green test proof, and fresh command output, but no durable evidence schema is implemented in the visible repository.

For resumability and auditability, add a small project-local evidence log/checkpoint model that records command, exit code, timestamp, summary, and relevant file/test references. If a host engine has its own checkpoint mechanism, it can mirror the same fields, but the portable source of truth should be file-backed.

---

## Question 8

**Context:** `using-git-worktrees` prefers native worktree tooling but does not name the APIs for each engine.
**Affected spec:** `_reversa_sdd/using-git-worktrees/design.md`, `_reversa_sdd/using-git-worktrees/tasks.md`
**Question:** Which native worktree tools, if any, should be recognized for Claude Code, Codex, Cursor, OpenCode, Gemini, Factory Droid, and GitHub Copilot CLI?
**Impact:** The answer determines whether manual git fallback is common or exceptional in the implementation.

✅ Responded and processed by Reviewer.

**Answer:** Recognize native worktree support opportunistically, not by assuming every engine has a named API.

- Claude Code: recognize `EnterWorktree` or equivalent native worktree tool when exposed.
- Codex App: recognize native managed workspaces/worktrees when exposed by the UI/runtime.
- Codex CLI: no repository-defined native API; use `git worktree` fallback unless a tool is exposed.
- Cursor: no repository-defined native worktree API; use extension/tool support if present, otherwise `git worktree`.
- OpenCode: no repository-defined native worktree API in the visible specs; use `git worktree` fallback unless its tool layer exposes one.
- Gemini CLI: no native worktree API is defined here; use `git worktree` fallback.
- Factory Droid and GitHub Copilot CLI: referenced only indirectly; treat native support as unknown and use `git worktree` fallback unless detected.

Implementation should detect available tool names/capabilities at runtime and prefer them over manual git commands.

---

## Question 9

**Context:** `writing-plans` bans placeholders, but no automated plan linter exists in the visible repository.
**Affected spec:** `_reversa_sdd/writing-plans/design.md`, `_reversa_sdd/writing-plans/tasks.md`
**Question:** Should placeholder detection remain a manual reviewer responsibility, or should the reimplementation include an automated lint/check script?
**Impact:** This affects implementation scope and quality gate enforcement.

✅ Responded and processed by Reviewer.

**Answer:** Include an automated lint/check script in the reimplementation, while keeping manual reviewer judgment as the final quality gate. The current `writing-plans` skill requires a self-review placeholder scan, but no visible automated linter enforces it.

The linter should catch explicit placeholder patterns such as `TBD`, `TODO`, `fill in`, `implement later`, `similar to`, and missing concrete commands/code in task steps. Manual review remains necessary for semantic placeholders such as vague error handling or underspecified tests that regex cannot reliably judge.

---

## Question 10

**Context:** `writing-skills` requires subagent pressure tests, but no universal pressure-test runner is defined.
**Affected spec:** `_reversa_sdd/writing-skills/design.md`, `_reversa_sdd/writing-skills/tasks.md`
**Question:** Should skill pressure testing be implemented with actual subagents, scripted prompt fixtures, or documented manual review?
**Impact:** This determines whether skill verification can be automated and reproduced across engines.

✅ Responded and processed by Reviewer.

**Answer:** Implement pressure testing with scripted prompt fixtures as the portable baseline, and run them through actual subagents where the host engine supports subagent dispatch. Actual subagents provide the strongest signal, but they are not universal across engines.

For engines without subagent APIs, the same fixtures should run as scripted conversations or documented manual review. The reproducible contract should be: baseline prompt, expected failure/rationalization, skill-enabled prompt, expected compliant behavior, and recorded pass/fail evidence.
