# C4 — Level 1: System Context

> Generated by Reversa Architect · 2026-05-15

---

## Diagram

```mermaid
C4Context
    title System Context — andrej-karpathy-skills

    Person(developer, "Developer", "Uses Claude Code for software engineering tasks")

    System(plugin, "andrej-karpathy-skills", "Claude Code skill plugin. Provides 4 behavioral principles that modify LLM coding behavior: Think Before Coding, Simplicity First, Surgical Changes, Goal-Driven Execution.")

    System_Ext(claudecode, "Claude Code", "Anthropic's AI coding assistant CLI and IDE extension. Reads skill content and injects it into the LLM context.")
    System_Ext(github, "GitHub", "Hosts the repository. Source for plugin installation and CLAUDE.md download.")
    System_Ext(marketplace, "Claude Code Marketplace", "Plugin discovery system. Allows users to find and install plugins via /plugin marketplace add.")
    System_Ext(skillssh, "skills.sh", "Community CLI tool for managing Claude Code skills. Reads skills/*/SKILL.md convention.", $tags="optional")

    Rel(developer, plugin, "Installs via /plugin install, curl, or skills.sh")
    Rel(plugin, claudecode, "Loaded as skill content into LLM context", "File read at session start")
    Rel(plugin, github, "Hosted on", "git/HTTPS")
    Rel(plugin, marketplace, "Listed in", "marketplace.json")
    Rel(developer, claudecode, "Uses for coding tasks", "CLI / IDE extension")
    Rel(claudecode, developer, "Applies guidelines during responses", "Modified LLM behavior")
    BiRel(marketplace, github, "Resolves plugin source from", "HTTPS")
```

---

## Notes

- 🟢 **CONFIRMADO** — All relationships confirmed from README.md, plugin.json, marketplace.json, and git history.
- 🟡 **INFERIDO** — The exact mechanism by which Claude Code injects skill content into the LLM context (file read timing, priority over CLAUDE.md, conflict resolution) is not documented in this repo.
- The "system" has no server, no process, and no network endpoint of its own — it is purely content that modifies an external system (Claude Code) at load time.
