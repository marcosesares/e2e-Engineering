# C4 — Level 2: Containers

> Generated by Reversa Architect · 2026-05-15

---

## Diagram

```mermaid
C4Container
    title Container Diagram — andrej-karpathy-skills

    Person(developer, "Developer", "Installs and uses the skill")

    System_Boundary(plugin, "andrej-karpathy-skills") {
        Container(skill_content, "Skill Content", "Markdown / SKILL.md", "Normative behavioral guidelines. The core deliverable. Loaded by Claude Code skill runtime.")
        Container(skill_content_zh, "Skill Content (zh)", "Markdown / SKILL.zh.md", "Simplified Chinese translation of the skill content.")
        Container(plugin_manifest, "Plugin Manifest", "JSON / plugin.json", "Claude Code plugin discovery metadata. Declares plugin identity, version, author, and skill paths.")
        Container(marketplace_listing, "Marketplace Listing", "JSON / marketplace.json", "Marketplace discoverability metadata. Declares owner, category, and description for /plugin marketplace add.")
        Container(project_config, "Project Config", "Markdown / CLAUDE.md", "Per-project install alternative. Mirrors Skill Content for users who prefer project-level injection over plugin installation.")
        Container(docs, "Documentation", "Markdown / README.md + EXAMPLES.md", "Human-facing project documentation, install instructions, and real-world usage examples.")
    }

    System_Ext(claudecode_runtime, "Claude Code Skill Runtime", "Reads SKILL.md from plugin directory. Injects content into LLM context at session start.")
    System_Ext(claudecode_project, "Claude Code Project Context", "Reads CLAUDE.md from project root. Injects content into LLM context per project.")
    System_Ext(marketplace, "Claude Code Marketplace")
    System_Ext(github, "GitHub")

    Rel(developer, plugin_manifest, "Installs via /plugin install", "Claude Code CLI")
    Rel(developer, marketplace_listing, "Discovers via /plugin marketplace add", "Claude Code CLI")
    Rel(developer, project_config, "Copies to project root", "curl / manual")
    Rel(developer, docs, "Reads installation and usage guide", "Browser / editor")

    Rel(plugin_manifest, skill_content, "References skill directory", "File path")
    Rel(marketplace_listing, plugin_manifest, "References plugin source", "Relative path ./")

    Rel(skill_content, claudecode_runtime, "Loaded into LLM context", "File read")
    Rel(skill_content_zh, claudecode_runtime, "Loaded into LLM context (zh locale)", "File read")
    Rel(project_config, claudecode_project, "Injected into project context", "File read")

    Rel(plugin_manifest, github, "Hosted on / installed from", "HTTPS")
    Rel(marketplace_listing, marketplace, "Listed in", "marketplace.json convention")
```

---

## Container Responsibilities

| Container | Normative? | Maintained by | Update frequency |
|-----------|-----------|---------------|-----------------|
| Skill Content | Yes — primary deliverable | repo owner + contributors | When guidelines change |
| Skill Content (zh) | Yes — localized copy | community (vtrois white) | 🔴 No sync process |
| Plugin Manifest | Yes — required for plugin install | repo owner + contributors | When version/structure changes |
| Marketplace Listing | Yes — required for marketplace | repo owner + contributors | When version changes |
| Project Config | Yes — per-project mirror | repo owner | Must stay in sync with Skill Content |
| Documentation | No — informational | repo owner + contributors | When install instructions change |

---

## Notes

- 🟢 **CONFIRMADO** — All containers confirmed from file structure analysis.
- 🔴 **LACUNA** — No automated sync between `CLAUDE.md` (Project Config) and `SKILL.md` (Skill Content). They can diverge silently.
- 🔴 **LACUNA** — `Skill Content (zh)` has no update trigger when `Skill Content` changes.
