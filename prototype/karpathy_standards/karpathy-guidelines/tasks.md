# Karpathy Guidelines, Implementation Tasks

> Spec generated by Reversa Writer · 2026-05-15
> Unit: `karpathy-guidelines` · Feature-level · doc_level: detalhado

---

## Prerequisites

- [ ] Target project has Claude Code installed
- [ ] Chosen install path decided: plugin or CLAUDE.md append
- [ ] For plugin path: `plugin.json` and `marketplace.json` are schema-valid

---

## Tasks

- [ ] T-01 — Author the four behavioral principles in `SKILL.md`
  - Origin: `skills/karpathy-guidelines/SKILL.md:1–55`
  - Criterion: All 21 business rules (BR-01–BR-21) present and readable by Claude Code runtime
  - Confidence: 🟢

- [ ] T-02 — Mirror `SKILL.md` content into `CLAUDE.md` at project root
  - Origin: `CLAUDE.md:1–63`
  - Criterion: `CLAUDE.md` content is functionally equivalent to `SKILL.md`; no behavioral divergence
  - Confidence: 🟢

- [ ] T-03 — Author `plugin.json` with correct `skills/karpathy-guidelines/` path
  - Origin: `.claude-plugin/plugin.json`
  - Criterion: File passes Claude Code plugin schema validation; `pluginPath` points to correct skill directory
  - Confidence: 🟢

- [ ] T-04 — Author `marketplace.json` for marketplace discoverability
  - Origin: `.claude-plugin/marketplace.json`
  - Criterion: File is valid JSON; `id`, `name`, `description`, `category` fields populated
  - Confidence: 🟢

- [ ] T-05 — Produce `EXAMPLES.md` with at least one concrete code example per principle
  - Origin: `EXAMPLES.md`
  - Criterion: Four sections, each with a before/after or annotated code block
  - Confidence: 🟢

- [ ] ~~T-06~~ — ~~Produce `SKILL.zh.md` (Simplified Chinese translation)~~ **Withdrawn** — maintainer decision 2026-05-15: translations not officially supported.

- [ ] ~~T-07~~ — ~~Mirror `CLAUDE.md` as `CLAUDE.zh.md`~~ **Withdrawn** — same decision as T-06.

- [ ] T-08 — Define semver policy for behavioral rule changes
  - Criterion: MAJOR = behavioral breaking change (rule removed/inverted); MINOR = rule added/tightened; PATCH = wording/formatting. Policy documented in CONTRIBUTING.md or README.md.
  - Confidence: 🟢 — confirmed by maintainer [Revisão: 🔴→🟢]

- [ ] T-09 — Build compliance test harness
  - Criterion: Test suite contains prompts for each of the 4 principles, expected behaviors, forbidden behaviors, and a rubric scoring mechanism. At minimum: one test prompt per BR that demonstrates the failure mode the rule prevents.
  - Confidence: 🟢 — confirmed by maintainer [Revisão: 🔴→🟢]

---

## Test Tasks

- [ ] TT-01 — Validate `plugin.json` against Claude Code plugin JSON schema
  - Criterion: Zero schema errors (regression introduced in `82467fa`, fixed in `68b67a5`)

- [ ] TT-02 — Validate `marketplace.json` against marketplace schema
  - Criterion: Zero schema errors

- [ ] TT-03 — Install via plugin path in a clean project; confirm all four principles appear in LLM context
  - Criterion: LLM cites a rule from each of the four principles when prompted

- [ ] TT-04 — Install via CLAUDE.md path; confirm same behavior as plugin path
  - Criterion: Functionally identical to TT-03

- [ ] ~~TT-05~~ — ~~Manually review `SKILL.zh.md`~~ **Withdrawn** — translations removed.

- [ ] TT-06 — Run compliance harness against each principle
  - Criterion: All 4 principles produce expected behavior in test outputs; forbidden behaviors do not appear

---

## Suggested Order

1. T-01 (normative source) → T-02 (mirror) — establish content before packaging
2. T-03, T-04 (manifests) — can run in parallel after T-01
3. T-05 (examples) — can run independently
4. T-06, T-07 (translations) — after T-01 and T-02 are final
5. TT-01–TT-05 — after all authoring tasks complete

---

## Open Gaps (🔴)

- No automated test harness to verify LLM compliance at runtime (L-03)
- No CI job to catch drift between `SKILL.md` and `SKILL.zh.md` (L-04)
- No semver policy for breaking changes to the rule set (L-02)
