# C4 — Level 3: Components

> Generated by Reversa Architect · 2026-05-15

---

## Focus Container: Skill Content (`skills/karpathy-guidelines/SKILL.md`)

This is the most architecturally relevant container — it is the system's sole executable unit (in the sense that it modifies LLM behavior). Its internal structure is analyzed here.

```mermaid
C4Component
    title Component Diagram — Skill Content (karpathy-guidelines)

    Container_Boundary(skill, "Skill Content — SKILL.md") {
        Component(meta_rule, "Meta-Rule", "Markdown section", "Global tradeoff: guidelines bias toward caution; trivial tasks may skip the full rigor. Frames all 4 principles.")
        
        Component(tbc, "Think Before Coding", "Markdown section", "Pre-condition guard procedure. 4 rules: surface assumptions, present interpretations, push back on over-complexity, stop when confused.")
        
        Component(sf, "Simplicity First", "Markdown section", "Code budget enforcement procedure. 5 prohibition rules + 1 senior engineer heuristic test.")
        
        Component(sc, "Surgical Changes", "Markdown section", "Diff scope minimizer. 4 edit-mode rules + 2 orphan-cleanup rules + 1 traceability test.")
        
        Component(gde, "Goal-Driven Execution", "Markdown section", "Task transformation procedure. Converts imperative tasks to verifiable goals. Includes multi-step plan pattern and verification loop.")
        
        Component(frontmatter, "Frontmatter", "YAML", "Skill identity metadata: name, description, license. Read by Claude Code skill loader.")
    }

    Container_Ext(claude_llm, "Claude LLM", "The language model that reads and applies all components as behavioral constraints.")
    Container_Ext(skill_loader, "Claude Code Skill Loader", "Reads SKILL.md, parses frontmatter, injects content into LLM system prompt.")

    Rel(skill_loader, frontmatter, "Reads identity metadata")
    Rel(skill_loader, claude_llm, "Injects full SKILL.md into LLM context")

    Rel(claude_llm, meta_rule, "Applies as global framing for all interactions")
    Rel(claude_llm, tbc, "Applies before any implementation begins")
    Rel(claude_llm, sf, "Applies during code generation")
    Rel(claude_llm, sc, "Applies when editing existing code")
    Rel(claude_llm, gde, "Applies to transform task statements into verifiable goals")

    Rel(tbc, sf, "On proceeding: hands off to code generation phase")
    Rel(tbc, gde, "On proceeding: task is ready for transformation to verifiable goal")
    Rel(gde, sc, "During execution loop: surgical changes applied to edits")
    Rel(gde, sf, "During execution loop: simplicity enforced on new code")
```

---

## Component Detail

| Component | Rules | Trigger condition | Output |
|-----------|-------|------------------|--------|
| Meta-Rule | 1 (BR-21) | Always active | Frames all other components |
| Think Before Coding | 4 (BR-01–04) | Any implementation request | Clarification, stated assumptions, or proceed |
| Simplicity First | 6 (BR-05–10) | Any code generation | Approved or simplified implementation |
| Surgical Changes | 7 (BR-11–17) | Any edit to existing code | Minimal diff |
| Goal-Driven Execution | 5 (BR-18–22) | Any task (especially multi-step) | Verifiable goal or step plan |

---

## Focus Container: Plugin Manifest (`.claude-plugin/plugin.json`)

```mermaid
C4Component
    title Component Diagram — Plugin Manifest

    Container_Boundary(manifest, "Plugin Manifest — plugin.json") {
        Component(identity, "Plugin Identity", "JSON fields: name, version, license", "Identifies the plugin to the Claude Code plugin system.")
        Component(author_block, "Author Block", "JSON object: author.name", "Identifies the publisher. Must be an object, not a plain string (ADR-006).")
        Component(skills_ref, "Skills Reference", "JSON array: skills", "Points to skill directories for auto-discovery. Must be directory paths, not file paths (ADR-006).")
        Component(keywords_block, "Keywords", "JSON array: keywords", "Discovery tags for search and filtering.")
    }

    Container_Ext(plugin_loader, "Claude Code Plugin Loader", "Reads .claude-plugin/plugin.json. Validates schema. Loads referenced skill directories.")
    Container_Ext(skill_content, "Skill Content", "skills/karpathy-guidelines/")

    Rel(plugin_loader, identity, "Validates plugin identity")
    Rel(plugin_loader, author_block, "Validates author object format")
    Rel(plugin_loader, skills_ref, "Resolves skill directories")
    Rel(skills_ref, skill_content, "References", "Relative path")
```

---

## Notes

- 🟢 **CONFIRMADO** — Component breakdown derived directly from SKILL.md section structure and plugin.json field analysis.
- 🟡 **INFERIDO** — The order of principle application (TBC → GDE → SF/SC) is inferred from the logical dependencies described in each principle. The Claude Code runtime does not enforce an order — the LLM applies them based on context.
- 🔴 **LACUNA** — The exact mechanism by which `meta_rule` overrides or qualifies the other components at inference time is not formally specified.
